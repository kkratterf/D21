import Empty from '@/components/ui/empty'

export function DirectoryNotFound() {
    return (
        <Empty
            title="No directory found"
            description="No directory found. But hey, don't give up! Try with another directory."
        />
    )
} 